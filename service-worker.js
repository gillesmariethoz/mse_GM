const cache='master-mse-online-v1';self.addEventListener('install',event=>event.waitUntil(caches.open(cache).then(store=>store.addAll(['./','index.html','manifest.json','icon.svg']))));self.addEventListener('activate',event=>event.waitUntil(self.clients.claim()));self.addEventListener('fetch',event=>event.respondWith(caches.match(event.request).then(hit=>hit||fetch(event.request))));
